# Test dependencies for Heimdal real-time monitoring
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
pytest-asyncio>=0.21.0
pytest-timeout>=2.1.0
aioresponses>=0.7.4
psutil>=5.9.0
coverage>=7.0.0

# For mocking HTTP responses in integration tests
responses>=0.23.0

# For performance monitoring
memory-profiler>=0.60.0

# For async testing
aiohttp>=3.8.0